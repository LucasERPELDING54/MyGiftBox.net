<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Prestation {{ id }}</title>
</head>
<body>
{%include "squellete/templateMain.html.twig" %}
    <ul>
    {% for presta in presta_liste %}
        <img src = "../../../shared/img/{{presta['img']}} width="200" height="200"">
        <a href="{{ presta.url }}">{{ presta.libelle }}</a>
        ({{ presta.tarif }}$ {{ presta.unite }} - id = {{ presta.id }})</li><br>  
    {% endfor %}
    
    </ul>
</body>
</html>